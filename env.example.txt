# ==============================================
# CONFIGURAÇÃO DO SISTEMA COMMAND-D
# ==============================================

# ----- BANCO DE DADOS DA LOCADORA (Local) -----
# Este é o banco de dados da LOCADORA (clientes, produtos, pedidos, etc)
DATABASE_URL="postgresql://usuario:senha@localhost:5432/nome_banco"

# ----- BANCO DE DADOS MASTER (Servidor do Otávio) -----
# Este é o banco do OTÁVIO que controla as licenças
# Apenas o servidor master do Otávio usa esta variável
MASTER_DATABASE_URL="postgresql://master_user:senha@master.command-d.com.br:5432/master_db"

# ----- LICENCIAMENTO -----
# Chaves fornecidas pelo Otávio ao contratar o sistema
LICENSE_API_KEY="cmd_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
LICENSE_API_SECRET="xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# URL do servidor master para validação de licença
MASTER_API_URL="https://master.command-d.com.br"

# ----- APLICAÇÃO -----
NODE_ENV="production"
PORT=8080
APP_VERSION="1.0.0"

# ----- JWT (Autenticação) -----
# Gerar com: openssl rand -hex 32
JWT_SECRET="sua_chave_secreta_super_segura_aqui"

# ----- EMAIL (Opcional) -----
# Para enviar notificações
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="seu-email@gmail.com"
SMTP_PASS="sua-senha-de-app"
SMTP_FROM="noreply@command-d.com.br"

# ----- STORAGE (Opcional) -----
# Para upload de arquivos (logos, fotos de produtos, etc)
AWS_S3_BUCKET="command-d-uploads"
AWS_S3_REGION="sa-east-1"
AWS_ACCESS_KEY_ID="AKIA..."
AWS_SECRET_ACCESS_KEY="..."

# ----- PAGAMENTOS (Opcional) -----
# Integração com gateway de pagamento
STRIPE_SECRET_KEY="sk_live_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# ----- NFSe (Opcional) -----
# Integração com prefeitura para emissão de nota fiscal
NFSE_AMBIENTE="producao" # ou "homologacao"
NFSE_CERTIFICADO_PATH="/caminho/para/certificado.pfx"
NFSE_CERTIFICADO_SENHA="senha_do_certificado"

# ----- FEATURES -----
# Habilitar/desabilitar funcionalidades
ENABLE_PARTNERSHIPS="false"  # Compartilhamento de clientes entre locadoras
ENABLE_HEARTBEAT="true"      # Enviar status para servidor master
ENABLE_AUTO_UPDATES="false"  # Atualizações automáticas
ENABLE_TELEMETRY="true"      # Métricas anônimas

# ----- DESENVOLVIMENTO -----
# Apenas para ambiente de desenvolvimento
DEBUG_MODE="false"
LOG_LEVEL="info" # debug, info, warn, error

